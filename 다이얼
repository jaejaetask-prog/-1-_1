word = input().strip()

dial = {
    'ABC':3, 'DEF':4, 'GHI':5,
    'JKL':6, 'MNO':7, 'PQRS':8,
    'TUV':9, 'WXYZ':10
}

time = 0

for w in word:
    for key in dial:
        if w in key:
            time += dial[key]

print(time)
